export enum ErrorCode {
  NotFound = 'instagram-post.not.found',
  BadRequest = 'instagram-post.bad.request',
  InternalServerError = 'instagram-post.internal.server.error',
  Unauthorized = 'instagram-post.unauthorized',
}

export enum ErrorDescription {
  NotFound = 'NotFound',
  Unauthorized = 'Unauthorized',
  BadRequest = 'BadRequest',
  InternalServerError = 'Internal Server Error',
}
